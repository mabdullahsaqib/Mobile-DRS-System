name: Dart (Flutter Build)

on:
  push:
    branches: [ "Staging-branch" ]
  pull_request:
    branches: [ "Staging-branch" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v3
        with:
          distribution: 'oracle'
          java-version: '17'

      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Install dependencies
        run: |
          cd frontend/mobile-drs-system
          flutter pub get

      - name: Build APK
        run: |
          cd frontend/mobile-drs-system
          flutter build apk --release

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: frontend/mobile-drs-system/build/app/outputs/flutter-apk/app-release.apk
